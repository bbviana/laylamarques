var React = require('react');
var PropTypes = React.PropTypes;

var Item = React.createClass({
    render() {
        return (
            <div>
                Item
            </div>
        );
    }
});

var _ = {
    container: {}
};

module.exports = Item;